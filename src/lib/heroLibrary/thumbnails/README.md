# Hero thumbnails

One PNG per hero, named `<slug>.png`, referenced from `catalog.json`.

The PNGs currently here are 1x1 transparent placeholders. In Phase 3 (picker UI),
replace each with a real screenshot: render the hero in the preview harness with
sample content + a neutral theme, capture at ~800x500, and save it under the same
filename so catalog.json needs no changes.
